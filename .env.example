# Template only. Inject these values into the service environment before rollout.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Select a database configuration. An explicit URL takes precedence.
# Keep the existing external mysql_config.py when using MYSQL_URL instead.
DATABASE_URL=
# Used only when DATABASE_URL and external mysql_config.py are absent:
DB_USER=
DB_PASS=
DB_HOST=
DB_NAME=
DB_PORT=5432

# Existing distributor environment settings; retain the deployment's values.
MERLION_CLIENT_ID=
MERLION_LOGIN=
MERLION_PASSWORD=
VVP_LOGIN=
VVP_PASSWORD=
